# Portfolio Frontend 🎨

لوحة تحكم متكاملة لإدارة البورتفوليو مبنية على Next.js 16 مع نظام مصادقة آمن ودعم كامل لـ Docker.

[![Next.js](https://img.shields.io/badge/Next.js-16-black)](https://nextjs.org/)
[![React](https://img.shields.io/badge/React-19-blue)](https://react.dev/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5-blue)](https://www.typescriptlang.org/)
[![Docker](https://img.shields.io/badge/Docker-Ready-blue)](https://www.docker.com/)
[![License](https://img.shields.io/badge/License-MIT-green)](LICENSE)

## 🌟 المميزات الرئيسية

- 🔐 **نظام مصادقة كامل** - JWT Authentication مع حماية الصفحات
- 🎨 **تصميم عصري** - نمط ألوان أسود وأبيض مع تباين عالي
- 🐳 **Docker Ready** - نشر سهل مع Docker و Docker Compose
- 🚀 **Multi-Port Support** - دعم تشغيل نسخ متعددة على بورتات مختلفة
- 📱 **Responsive Design** - تصميم متجاوب يعمل على جميع الأجهزة
- 🌐 **دعم كامل للعربية** - RTL Support
- 📊 **لوحة تحكم شاملة** - إدارة المشاريع، العملاء، والرسائل

## ⚡ المميزات

- ✅ نظام مصادقة كامل (تسجيل دخول، تسجيل، تسجيل خروج)
- ✅ JWT Authentication
- ✅ حماية الصفحات (Protected Routes)
- ✅ إدارة الحالة باستخدام Context API
- ✅ تصميم متجاوب (Responsive Design)
- ✅ دعم اللغة العربية (RTL)
- ✅ التحقق من صحة البيانات
- ✅ رسائل خطأ واضحة
- ✅ مؤشرات تحميل (Loading States)

## 🛠️ التقنيات المستخدمة

- **Next.js 16** - React Framework
- **TypeScript** - Type Safety
- **Tailwind CSS 4** - Styling
- **Context API** - State Management
- **JWT** - Authentication

## 📋 المتطلبات

- Node.js 18+
- npm أو yarn أو pnpm
- Backend API يعمل على المنفذ 5000

## 🚀 Quick Start

### الطريقة الأولى: Docker (موصى به) 🐳

```bash
# 1. استنساخ المشروع
git clone https://github.com/DeemaAlothman/portfolio-frontend.git
cd portfolio-frontend

# 2. إعداد متغيرات البيئة
cp .env.example .env.production
# عدّل .env.production بمعلوماتك

# 3. بناء وتشغيل
docker-compose up -d

# 4. التطبيق متاح على
# http://localhost:3010
```

### الطريقة الثانية: Development Mode 💻

```bash
# 1. تثبيت المكتبات
npm install

# 2. إعداد متغيرات البيئة
cp .env.example .env.local

# 3. تشغيل السيرفر
npm run dev

# 4. افتح المتصفح على
# http://localhost:3000
```

## 🐳 النشر على السيرفر

### الخطوة 1: رفع الملفات للسيرفر

```bash
# ضغط المشروع
tar -czf portfolio-frontend.tar.gz .

# رفع للسيرفر
scp portfolio-frontend.tar.gz root@YOUR_SERVER_IP:/root/

# الاتصال بالسيرفر
ssh root@YOUR_SERVER_IP

# فك الضغط
cd /root
mkdir -p portfolio-frontend
tar -xzf portfolio-frontend.tar.gz -C portfolio-frontend/
cd portfolio-frontend
```

### الخطوة 2: النشر

```bash
# إعطاء صلاحيات التنفيذ
chmod +x deploy.sh check-conflicts.sh port-manager.sh

# تشغيل النشر (مع فحص تلقائي للتضارب)
./deploy.sh
```

### الخطوة 3: إدارة البورتات (اختياري)

```bash
# استخدام مدير البورتات التفاعلي
./port-manager.sh
```

**للمزيد من التفاصيل:**
- 📖 [دليل النشر الكامل](DEPLOYMENT.md)
- 🔌 [دليل إدارة البورتات](PORTS_GUIDE.md)
- 🛡️ [ضمان عدم التضارب](NO_CONFLICTS_GUARANTEED.md)

## 📁 هيكل المشروع

```
portfolio-frontend/
├── src/
│   ├── app/
│   │   ├── auth/              # صفحات المصادقة
│   │   ├── dashboard/         # لوحة التحكم (Admin)
│   │   ├── portfolio/         # صفحات البورتفوليو العامة
│   │   ├── clients/           # صفحات العملاء
│   │   └── contact/           # صفحة التواصل
│   ├── components/
│   │   ├── auth/              # مكونات المصادقة
│   │   ├── layout/            # مكونات التخطيط
│   │   ├── works/             # مكونات إدارة المشاريع
│   │   ├── clients/           # مكونات إدارة العملاء
│   │   └── public/            # المكونات العامة
│   ├── contexts/
│   │   └── AuthContext.tsx    # سياق المصادقة
│   └── lib/
│       ├── api.ts             # خدمات API الأساسية
│       └── services/          # خدمات API المتخصصة
├── Docker Files/
│   ├── Dockerfile             # ملف Docker
│   ├── docker-compose.yml     # Docker Compose
│   ├── docker-compose.multi-port.yml  # نسخ متعددة
│   └── .dockerignore          # تجاهل الملفات
├── Deployment Scripts/
│   ├── deploy.sh              # سكريبت النشر الرئيسي
│   ├── check-conflicts.sh     # فحص التضارب
│   └── port-manager.sh        # إدارة البورتات
├── Documentation/
│   ├── DEPLOYMENT.md          # دليل النشر
│   ├── PORTS_GUIDE.md         # دليل البورتات
│   └── NO_CONFLICTS_GUARANTEED.md
└── .env.example               # نموذج متغيرات البيئة
```

## 🔑 API Endpoints المطلوبة

يجب أن يوفر Backend API الخاص بك هذه الـ Endpoints:

### POST /auth/register
```json
// Request
{
  "name": "Admin User",
  "email": "admin@example.com",
  "password": "password123",
  "role": "ADMIN"
}

// Response
{
  "token": "jwt_token_here",
  "user": {
    "id": "1",
    "email": "admin@example.com",
    "name": "Admin User",
    "role": "ADMIN"
  }
}
```

### POST /auth/login
```json
// Request
{
  "email": "admin@example.com",
  "password": "password123"
}

// Response
{
  "token": "jwt_token_here",
  "user": {
    "id": "1",
    "email": "admin@example.com",
    "name": "Admin User",
    "role": "ADMIN"
  }
}
```

### GET /auth/me
```json
// Headers
{
  "Authorization": "Bearer jwt_token_here"
}

// Response
{
  "id": "1",
  "email": "admin@example.com",
  "name": "Admin User",
  "role": "ADMIN",
  "createdAt": "2024-01-01T00:00:00.000Z"
}
```

## 🎯 الاستخدام

### 1. الصفحة الرئيسية
- عرض مقدمة عن النظام
- أزرار التنقل لتسجيل الدخول أو إنشاء حساب

### 2. التسجيل (/auth/register)
- إنشاء حساب جديد
- التحقق من صحة البيانات
- إعادة التوجيه التلقائي للوحة التحكم

### 3. تسجيل الدخول (/auth/login)
- تسجيل الدخول بالبريد الإلكتروني وكلمة المرور
- حفظ الـ Token في LocalStorage
- إعادة التوجيه للوحة التحكم

### 4. لوحة التحكم (/dashboard)
- صفحة محمية تتطلب تسجيل الدخول
- عرض معلومات المستخدم
- زر تسجيل الخروج

## 🔒 الأمان

- جميع الصفحات المحمية تتطلب JWT صالح
- التحقق من صحة البيانات في الواجهة الأمامية
- تخزين آمن للـ Token في LocalStorage
- إعادة توجيه تلقائية للمستخدمين غير المصادقين

## 🎨 التخصيص

### تغيير الألوان

عدّل ملف [src/app/globals.css](src/app/globals.css:1):

```css
:root {
  --background: #000000;
  --foreground: #ffffff;
  --primary: #ffffff;
  --secondary: #1a1a1a;
  --accent: #383838;
  --border: rgba(255, 255, 255, 0.145);
  --error: #ef4444;
  --success: #10b981;
}
```

## 📝 ملاحظات

- تأكد من تشغيل Backend API قبل استخدام التطبيق
- الـ Token يُحفظ في LocalStorage ويبقى حتى تسجيل الخروج
- النظام يدعم اللغة العربية بالكامل مع RTL

## 🐛 المشاكل الشائعة

### خطأ في الاتصال بالـ API
تأكد من:
1. تشغيل Backend API على المنفذ الصحيح
2. تحديث `NEXT_PUBLIC_API_URL` في `.env.local`
3. تفعيل CORS في Backend API

### الصفحة لا تعيد التوجيه
1. امسح LocalStorage في المتصفح
2. أعد تحميل الصفحة
3. تأكد من صحة الـ Token

## 📄 الترخيص

MIT License

## 👨‍💻 التطوير

تم بناء هذا المشروع باستخدام أحدث تقنيات الويب مع التركيز على الأداء وتجربة المستخدم.
