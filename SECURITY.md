# Security Policy 🔒

## ملفات حساسة - لا ترفعها على GitHub!

### ⚠️ تحذير مهم جداً

الملفات التالية تحتوي على معلومات حساسة ويجب **عدم رفعها على GitHub أبداً**:

- `.env.production` - يحتوي على بيانات السيرفر وكلمات المرور
- `.env.local` - إعدادات البيئة المحلية
- أي ملف `.env*` (محمي تلقائياً في `.gitignore`)

### ✅ ما تم حمايته

تم إضافة هذه الملفات إلى `.gitignore`:

```
.env*
.env.production
.env.local
.env.development.local
.env.test.local
.env.production.local
```

### 📝 استخدام `.env.example`

ملف `.env.example` هو نموذج آمن **لا يحتوي** على معلومات حساسة.

للاستخدام:
```bash
# نسخ النموذج
cp .env.example .env.production

# تعديل الملف بمعلوماتك الحقيقية
nano .env.production
```

### 🔐 المعلومات الحساسة

**لا تشارك أبداً:**
- كلمات مرور السيرفر
- عناوين IP خاصة
- مفاتيح API
- كلمات مرور البريد الإلكتروني
- JWT Secrets
- Database credentials

### 🛡️ أفضل الممارسات

1. **استخدم متغيرات البيئة على السيرفر**
   ```bash
   export DB_PASSWORD="your_password"
   ```

2. **لا تحفظ كلمات المرور في الكود**
   ```javascript
   // ❌ خطأ
   const password = "mypassword123";
   
   // ✅ صحيح
   const password = process.env.DB_PASSWORD;
   ```

3. **غيّر كلمات المرور بانتظام**

4. **استخدم أدوات مثل:**
   - [git-secrets](https://github.com/awslabs/git-secrets)
   - [gitleaks](https://github.com/zricethezav/gitleaks)
   - [detect-secrets](https://github.com/Yelp/detect-secrets)

### 🚨 في حالة تسريب معلومات حساسة

إذا رفعت معلومات حساسة عن طريق الخطأ:

1. **غيّر جميع كلمات المرور فوراً**
2. **احذف الـ commit من التاريخ:**
   ```bash
   git filter-branch --force --index-filter \
     "git rm --cached --ignore-unmatch .env.production" \
     --prune-empty --tag-name-filter cat -- --all
   
   git push origin --force --all
   ```
3. **أبلغ فريقك**
4. **راجع سجلات الأمان**

### 📞 الإبلاغ عن مشكلة أمنية

إذا وجدت ثغرة أمنية، يرجى:
- عدم مشاركتها علناً
- التواصل مع المطورين مباشرة
- إعطاء وقت كافٍ لإصلاح المشكلة

## Supported Versions

| Version | Supported          |
| ------- | ------------------ |
| 1.x     | :white_check_mark: |

## Reporting a Vulnerability

Please report security vulnerabilities to: [mohammed.besher.hamzeh@gmail.com]

**لا ترفع Issue عامة للثغرات الأمنية!**
