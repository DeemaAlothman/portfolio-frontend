# إعدادات رفع الملفات (Upload Settings) 📤

## ⏱️ الإجابة على سؤالك:

> **"لما عم برفع ريلز شقد اكتر مدة ممكن نرفعها؟"**

**الإعدادات الحالية:**

| الإعداد | القيمة | الشرح |
|---------|--------|-------|
| **Timeout** | 10 دقائق (600 ثانية) | الوقت الأقصى للرفع |
| **حجم الملف** | 100MB | الحد الأقصى لحجم الملف |
| **سرعة النت** | أي سرعة | يشتغل حتى لو النت بطيء |

---

## ✅ نعم، رح يرفع حتى لو النت بطيء!

**مع الإعدادات الجديدة:**
- ✅ لو النت بطيء → السيرفر **ينتظر** حتى 10 دقائق
- ✅ الملف رح يرفع بالنهاية طالما تم خلال 10 دقائق
- ✅ إذا تخطى 10 دقائق → **فشل**

---

## 📊 أمثلة عملية:

### مثال 1: نت سريع
```
حجم الفيديو: 50MB
سرعة النت: 10 Mbps
وقت الرفع: ~40 ثانية
النتيجة: ✅ رفع ناجح
```

### مثال 2: نت متوسط
```
حجم الفيديو: 50MB
سرعة النت: 2 Mbps
وقت الرفع: ~3 دقائق
النتيجة: ✅ رفع ناجح
```

### مثال 3: نت بطيء
```
حجم الفيديو: 50MB
سرعة النت: 512 Kbps
وقت الرفع: ~13 دقائق
النتيجة: ❌ فشل (تجاوز 10 دقائق)
```

---

## ⚙️ الإعدادات القابلة للتعديل

### 1. زيادة الـ Timeout (الوقت الأقصى)

في ملف `src/app/api/upload/route.ts`:

```typescript
// ⏱️ الوقت الحالي: 10 دقائق
export const maxDuration = 600; // 600 ثانية

// 💡 يمكنك زيادته:
export const maxDuration = 1200; // 20 دقيقة
export const maxDuration = 1800; // 30 دقيقة
```

---

### 2. زيادة حجم الملف المسموح

في ملف `src/app/api/upload/route.ts`:

```typescript
// 📦 الحجم الحالي: 100MB
export const config = {
  api: {
    bodyParser: {
      sizeLimit: '100mb',
    },
  },
};

// 💡 يمكنك زيادته:
sizeLimit: '200mb', // 200 ميجا
sizeLimit: '500mb', // 500 ميجا
sizeLimit: '1gb',   // 1 جيجا
```

---

## 🎬 توصيات لرفع الريلز:

### ✅ الأحجام الموصى بها:

| نوع المحتوى | الحجم المثالي | الحجم الأقصى |
|-------------|---------------|---------------|
| ريلز قصير (15-30 ثانية) | 10-20MB | 50MB |
| ريلز متوسط (30-60 ثانية) | 20-40MB | 80MB |
| ريلز طويل (1-3 دقائق) | 40-80MB | 100MB |

---

### 💡 نصائح لتسريع الرفع:

#### 1. ضغط الفيديو قبل الرفع
استخدم أدوات مثل:
- **HandBrake** (مجاني)
- **Adobe Media Encoder**
- **Online Video Compressor**

**مثال:**
```
قبل الضغط: 150MB
بعد الضغط: 40MB
الجودة: نفسها تقريباً
```

---

#### 2. استخدام إعدادات تصدير مناسبة:

```
الدقة: 1080p (1920x1080)
معدل الإطارات: 30 fps
الترميز: H.264
البت ريت: 5-8 Mbps
```

---

#### 3. رفع على YouTube ثم استخدام الرابط

إذا الفيديو كبير جداً:

```
1. ارفع الفيديو على YouTube
2. في لوحة التحكم، استخدم رابط YouTube
3. أسرع وأوفر مساحة على السيرفر
```

---

## 🔧 إعدادات السيرفر

### على Docker:

في ملف `docker-compose.yml`، يمكن زيادة الـ timeout:

```yaml
services:
  portfolio-frontend:
    environment:
      - BODY_SIZE_LIMIT=100mb
      - REQUEST_TIMEOUT=600000  # 10 دقائق بالميلي ثانية
```

---

### على Nginx (إذا تستخدم):

في ملف إعدادات Nginx:

```nginx
http {
    # زيادة حجم الملف المسموح
    client_max_body_size 100M;

    # زيادة الـ timeout
    client_body_timeout 600s;
    proxy_read_timeout 600s;
}
```

---

## 📈 جدول مقارنة السرعات:

| سرعة النت | 10MB | 50MB | 100MB |
|-----------|------|------|-------|
| 1 Mbps | 80 ثانية | 6.5 دقيقة | 13 دقيقة ⚠️ |
| 2 Mbps | 40 ثانية | 3.3 دقيقة | 6.6 دقيقة ✅ |
| 5 Mbps | 16 ثانية | 80 ثانية | 2.6 دقيقة ✅ |
| 10 Mbps | 8 ثوانٍ | 40 ثانية | 80 ثانية ✅ |

⚠️ = قد يفشل إذا الـ timeout 10 دقائق
✅ = آمن

---

## 🚨 ماذا يحدث عند تجاوز الـ Timeout؟

### السيناريو:

```
1. المستخدم يبدأ رفع فيديو 80MB
2. النت بطيء (500 Kbps)
3. وقت الرفع المتوقع: 21 دقيقة
4. الـ timeout: 10 دقائق
```

### النتيجة:

```
⏱️ بعد 10 دقائق:
❌ السيرفر يوقف الـ request
❌ رسالة خطأ: "Upload timeout"
❌ الملف لا يتم حفظه
```

### الحل:

```
1. ضغط الفيديو لحجم أصغر
2. زيادة الـ timeout في الإعدادات
3. استخدام رابط YouTube بدلاً من الرفع المباشر
```

---

## ⚡ الحل الأمثل للريلز:

### الخيار 1: رفع مباشر (للفيديوهات الصغيرة)

```
الحجم: أقل من 50MB
الوقت: أقل من 5 دقائق
الطريقة: رفع مباشر من لوحة التحكم
```

### الخيار 2: YouTube/Vimeo (للفيديوهات الكبيرة)

```
الحجم: أي حجم
الوقت: لا محدود
الطريقة:
1. ارفع على YouTube
2. خلي الفيديو "غير مدرج" (Unlisted)
3. انسخ الرابط
4. الصقه في لوحة التحكم
```

**المميزات:**
- ✅ بدون حد للحجم أو الوقت
- ✅ لا يستهلك مساحة السيرفر
- ✅ سرعة تحميل أعلى للزوار
- ✅ يدعم جودات متعددة تلقائياً

---

## 🛠️ خطوات تطبيق الإعدادات:

### 1. على السيرفر:

```bash
# 1. سحب آخر التحديثات
cd /root/portfolio-frontend
git pull

# 2. إعادة بناء Docker
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# 3. التحقق من الإعدادات
docker-compose logs -f
```

### 2. محلياً (للتطوير):

```bash
# 1. سحب التحديثات
git pull

# 2. إعادة تشغيل السيرفر
npm run dev
```

---

## 📊 الخلاصة:

### الإعدادات الحالية:

```
✅ الحد الأقصى للوقت: 10 دقائق
✅ الحد الأقصى للحجم: 100MB
✅ يعمل مع النت البطيء: نعم
✅ يفشل إذا: تجاوز 10 دقائق
```

### التوصيات:

1. **للريلز حتى 50MB:**
   - ✅ الرفع المباشر آمن
   - ⏱️ يشتغل حتى مع نت بطيء

2. **للريلز أكبر من 50MB:**
   - ⚠️ ضغط الفيديو أولاً
   - 🎥 أو استخدام YouTube

3. **للريلز أكبر من 100MB:**
   - 🎬 استخدام YouTube (الأفضل)
   - 📈 أو زيادة الـ timeout والحجم الأقصى

---

## 🔗 روابط مفيدة:

- **ضغط الفيديو:** https://www.handbrake.fr/
- **ضغط أونلاين:** https://www.freeconvert.com/video-compressor
- **YouTube Upload:** https://www.youtube.com/upload

---

**نتمنى لك رفع سريع وسهل! 🚀**
