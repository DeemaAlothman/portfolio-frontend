# Works Module - قسم الأعمال

## ✅ الميزات المكتملة

### 1. صفحة قائمة الأعمال (`/dashboard/works`)
- عرض جميع الأعمال في Grid متجاوب
- فلترة حسب:
  - نوع العمل (LOGO, WEBSITE, SOCIAL_MEDIA, REEL)
  - الحالة (PUBLISHED, DRAFT, ARCHIVED)
- عرض صورة العمل الأساسية
- عرض معلومات العمل (العنوان، النوع، العميل، الوصف)
- أزرار التعديل والحذف
- تأكيد الحذف

### 2. صفحة إضافة عمل جديد (`/dashboard/works/new`)
- فورم شامل لإضافة عمل:
  - معلومات أساسية (العنوان، النوع، الحالة، العميل)
  - وصف قصير وعنوان فرعي
  - تاريخ النشر ورابط الزيارة
  - خيار العمل المميز
  - إعدادات SEO (عنوان، وصف، كلمات مفتاحية)
  - رفع صورة مع معاينة
- التحقق من صحة البيانات
- رسائل خطأ واضحة

### 3. صفحة تعديل العمل (`/dashboard/works/[id]/edit`)
- تحميل بيانات العمل الحالية
- نفس فورم الإضافة مع بيانات محملة مسبقاً
- معاينة الصورة الحالية

### 4. API Service (`worksAPI.ts`)
- جميع العمليات CRUD:
  - `getAll()` - قائمة الأعمال مع فلاتر
  - `getBySlug()` - الحصول على عمل بواسطة slug
  - `getById()` - الحصول على عمل بواسطة ID
  - `create()` - إنشاء عمل جديد
  - `update()` - تحديث عمل
  - `delete()` - حذف عمل
- دعم رفع الملفات (FormData)
- معالجة الأخطاء بشكل مركزي

## 📁 هيكل الملفات

```
src/
├── app/
│   └── dashboard/
│       └── works/
│           ├── page.tsx              # قائمة الأعمال
│           ├── new/
│           │   └── page.tsx          # إضافة عمل
│           └── [id]/
│               └── edit/
│                   └── page.tsx      # تعديل عمل
├── components/
│   └── works/
│       └── WorkForm.tsx              # فورم العمل (مشترك)
└── lib/
    └── services/
        └── worksAPI.ts               # خدمة API
```

## 🎨 التصميم

- نظام ألوان بيج/تيل متناسق
- مكونات قابلة لإعادة الاستخدام
- UI متجاوب (Responsive)
- تأثيرات Hover و Transitions سلسة
- رسائل حالة واضحة (Loading, Error, Success)

## 🔌 Backend Integration

يتكامل مع endpoints التالية:

```
GET    /works               # قائمة الأعمال
GET    /works/:slug         # عمل بواسطة slug
GET    /works/id/:id        # عمل بواسطة ID
POST   /works               # إنشاء عمل
PATCH  /works/:id           # تحديث عمل
DELETE /works/:id           # حذف عمل
```

## 🚀 الاستخدام

1. من لوحة التحكم، اضغط على بطاقة "الأعمال"
2. اضغط "+ إضافة عمل جديد" لإنشاء عمل
3. املأ البيانات وارفع صورة
4. اضغط "إنشاء العمل"
5. استخدم الفلاتر للبحث في الأعمال
6. اضغط "تعديل" لتحديث عمل موجود
7. اضغط "حذف" لحذف عمل (مع تأكيد)

## ⚠️ ملاحظات مهمة

### 1. Client ID
حالياً يتم إدخال `clientId` يدوياً. يمكن تحسينه لاحقاً بإضافة:
- Dropdown لاختيار العميل من قائمة
- Auto-complete للبحث عن العملاء
- زر لإضافة عميل جديد

### 2. Tags
تم تحضير حقل `tagIds` في API لكن لم يتم إضافة واجهة اختيار Tags بعد.

### 3. Upload Progress
لا يوجد شريط تقدم لرفع الملفات حالياً. يمكن إضافته لاحقاً.

### 4. Image Optimization
الصور تُرفع كما هي. يمكن إضافة:
- Compression قبل الرفع
- Multiple sizes/thumbnails
- Lazy loading

## 🔮 تحسينات مستقبلية

- [ ] إضافة pagination للقائمة
- [ ] Search bar للبحث في العناوين
- [ ] Drag & drop لرفع الصور
- [ ] Multiple images support
- [ ] نظام Sections الموجود في Backend
- [ ] Media gallery لكل عمل
- [ ] Bulk actions (حذف، تعديل حالة لعدة أعمال)
- [ ] Export/Import للأعمال
- [ ] نظام Revisions للتراجع عن التعديلات
- [ ] Preview mode قبل النشر

## 🐛 استكشاف الأخطاء

### خطأ "Client not found"
- تأكد من وجود عميل بالـ ID المدخل في قاعدة البيانات

### خطأ في رفع الصورة
- تأكد من أن Backend يدعم multipart/form-data
- تحقق من حجم الملف (الحد الأقصى 20MB)
- تأكد من أن مجلد `uploads` موجود في Backend

### الصور لا تظهر
- تأكد من تفعيل serve static files في Backend
- تحقق من المسار الصحيح للصور
- افتح Network tab في DevTools لرؤية الأخطاء

## 📚 Resources

- [Backend API Documentation](../backend/README.md)
- [Prisma Schema](../backend/prisma/schema.prisma)
- [Next.js App Router](https://nextjs.org/docs/app)
